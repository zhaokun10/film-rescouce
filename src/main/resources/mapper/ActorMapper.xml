<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sht.filmrescource.mapper.ActorMapper">

    <resultMap type="com.sht.filmrescource.entity.Actor" id="ActorMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="actorName" column="actor_name" jdbcType="VARCHAR"/>
        <result property="actorProfile" column="actor_profile" jdbcType="VARCHAR"/>
        <result property="duties" column="duties" jdbcType="INTEGER"/>
        <result property="actorCountry" column="actor_country" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ActorMap">
        select id,
               actor_name,
               actor_profile,
               duties,
               country
        from actor
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ActorMap">
        select
        id, actor_name, actor_profile, duties, country
        from actor
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="actorName != null and actorName != ''">
                and actor_name = #{actorName}
            </if>
            <if test="actorProfile != null and actorProfile != ''">
                and actor_profile = #{actorProfile}
            </if>
            <if test="duties != null">
                and duties = #{duties}
            </if>
            <if test="country != null">
                and country = #{country}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from actor
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="actorName != null and actorName != ''">
                and actor_name = #{actorName}
            </if>
            <if test="actorProfile != null and actorProfile != ''">
                and actor_profile = #{actorProfile}
            </if>
            <if test="duties != null">
                and duties = #{duties}
            </if>
            <if test="country != null">
                and country = #{country}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into actor(actor_name, actor_profile, duties, country)
        values (#{actorName}, #{actorProfile}, #{duties}, #{country})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into actor(actor_name, actor_profile, duties, country)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.actorName}, #{entity.actorProfile}, #{entity.duties}, #{entity.country})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into actor(actor_name, actor_profile, duties, country)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.actorName}, #{entity.actorProfile}, #{entity.duties}, #{entity.country})
        </foreach>
        on duplicate key update
        actor_name = values(actor_name),
        actor_profile = values(actor_profile),
        duties = values(duties),
        country = values(country)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update actor
        <set>
            <if test="actorName != null and actorName != ''">
                actor_name = #{actorName},
            </if>
            <if test="actorProfile != null and actorProfile != ''">
                actor_profile = #{actorProfile},
            </if>
            <if test="duties != null">
                duties = #{duties},
            </if>
            <if test="country != null">
                country = #{country},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from actor
        where id = #{id}
    </delete>

</mapper>

