<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sht.filmrescource.mapper.RoleMapper">

    <resultMap type="com.sht.filmrescource.entity.Role" id="RoleMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="roleName" column="role_name" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoleMap">
        select
          id, role_name
        from role
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RoleMap">
        select
          id, role_name
        from role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="roleName != null">
                and role_name = #{roleName}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="roleName != null">
                and role_name = #{roleName}
            </if>
        </where>
    </select>
    <select id="findRoleByUserId" resultType="java.lang.Long">
        select role_id from user_role_xref where user_id = #{userId,jdbcType=BIGINT}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into role(role_name)
        values (#{roleName})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into role(role_name)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.roleName})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into role(role_name)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.roleName})
        </foreach>
        on duplicate key update
        role_name = values(role_name)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update role
        <set>
            <if test="roleName != null">
                role_name = #{roleName},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from role where id = #{id}
    </delete>

</mapper>

